<UserControl x:Class="cloud.MVVM.View.FileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:cloud.MVVM.View"
             xmlns:extensions="clr-namespace:cloud.Core" 
             xmlns:viewmodel="clr-namespace:cloud.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#ffffff">
    <Grid>

        <StackPanel VerticalAlignment="Center"
                   HorizontalAlignment="Center">
            <TextBlock Text="Zaloguj się aby mieć dostęp do swoich plików"
                   FontSize="30"
                   Foreground="Black"
                   Margin="0,0,0,20"/>
            <Button Content="Przejdź do logowania" 
                    Width="300"
                    Height="50"
                    Command="{Binding DataContext.ShowLoginView,RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                    Style="{StaticResource AllButtonsStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
